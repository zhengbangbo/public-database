on:
  schedule:
    - cron:  '*/5 * * * *'
    
jobs:
  updateNotionDatabase:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - uses: pnpm/action-setup@v2.2.4
        with:
          version: latest
      - run: pnpm i
      - run: pnpm start
